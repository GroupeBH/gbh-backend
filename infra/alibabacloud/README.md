# Alibaba Cloud

This provider directory is intentionally empty for now.

The placeholders keep the final structure ready for Terraform, Ansible, and the
`alibabacloud.yml` workflow when the deployment target is defined.

